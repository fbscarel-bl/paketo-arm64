name: create-manifest

on:
  workflow_dispatch:
#  push:
#    branches:
#      - main

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
      - name: Login to DockerHub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: create-manifest
        run: |
          #Cleanup
          docker manifest create dashaun/builder-alt:tiny --amend dashaun/builder-alt-arm:tiny --amend paketobuildpacks/builder:tiny
          docker manifest push dashaun/builder-alt:tiny